---
title: contact_merge_same_contact
description: Contact Merge Same Contact
---

## When this happens

A domain rule blocked the operation after validation. Affects **Contacts** operations.

## What your integration should do

Verify contact ids, required body fields, and that the record belongs to the authenticated user.

## What end users should see

Translate with `common.errors.api.contact_merge_same_contact` when a string exists; otherwise map by domain prefix. Never surface the raw API `message`.

## Response fields

| Property | Value |
|----------|-------|
| **Type** | `invalid_request_error` |
| **HTTP status** | 400 |
| **code** | `contact_merge_same_contact` |
| **doc_url** | `/docs/api/errors/contact_merge_same_contact` |

The API `message` field is for developers debugging in logs. Product UI must key off `code` (and `details` for validation).

## Source references

- `apps/api/src/domains/contacts/merge.ts`
