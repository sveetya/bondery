---
title: interaction_interaction_was_created_but_could_not_be
description: Interaction Interaction Was Created But Could Not Be
---

## When this happens

An unexpected failure occurred while handling the request. Affects **Interactions** operations.

## What your integration should do

Log the response `request_id` if present and retry once. Escalate if the error persists — likely a server-side bug or dependency outage.

## What end users should see

Show generic retry copy (`common.errors.api.internal` or `common.errors.apiTemporarilyUnavailable`).

## Response fields

| Property | Value |
|----------|-------|
| **Type** | `api_error` |
| **HTTP status** | 500 |
| **code** | `interaction_interaction_was_created_but_could_not_be` |
| **doc_url** | `/docs/api/errors/interaction_interaction_was_created_but_could_not_be` |

The API `message` field is for developers debugging in logs. Product UI must key off `code` (and `details` for validation).

## Source references

- `apps/api/src/services/interactions/index.ts`
