/**
 * ChatClient is mounted in the layout, so a route-level skeleton would stack
 * on top of the live pane during /app/chat ↔ /app/chat/[sessionId] navigation.
 */
export default function ChatLoading() {
  return null;
}
