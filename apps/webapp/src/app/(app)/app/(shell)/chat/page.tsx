/**
 * Chat index page — ChatClient lives in the layout so it stays mounted
 * across /app/chat and /app/chat/[sessionId]. A new session is only created
 * when the user sends their first message.
 */
export default function ChatPage() {
  return null;
}
