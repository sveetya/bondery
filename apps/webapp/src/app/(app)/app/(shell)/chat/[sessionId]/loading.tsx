/**
 * ChatClient is mounted in the parent layout, so a route-level skeleton would
 * stack on top of the live pane while this segment loads.
 */
export default function ChatSessionLoading() {
  return null;
}
