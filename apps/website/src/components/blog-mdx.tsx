/**
 * Blog MDX styling lives in the root `mdx-components.tsx` file.
 * Next.js applies `useMDXComponents` automatically for all compiled `.mdx` files.
 */
export { useMDXComponents as blogMdxComponents } from "../../mdx-components";
