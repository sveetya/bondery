import { GithubInfo } from "fumadocs-ui/components/github-info";

/** Bondery monorepo card — use for repository mentions in docs. */
export function BonderyRepo() {
  return <GithubInfo owner="usebondery" repo="bondery" />;
}
