/** Standard Open Graph / Twitter large-image canvas size. */
export const OG_SIZE = {
  height: 630,
  width: 1200,
} as const;

/** Brand gradient stops (matches logotype icon fill). */
export const OG_GRADIENT_FROM = "#7C3AED";
export const OG_GRADIENT_TO = "#C026D3";

/** Shared light brand wash used by all OG layouts. */
export const OG_BACKGROUND =
  "linear-gradient(135deg, #ffffff 0%, #faf5ff 55%, #fce7f3 100%)";

export const OG_SITE_NAME = "Bondery";

/** Default tagline on marketing OG cards (website + webapp root). */
export const OG_DEFAULT_TAGLINE = "Build bonds that last forever.";

/** Matches the website body font (next/font Lexend). */
export const OG_FONT_FAMILY = "Lexend";
